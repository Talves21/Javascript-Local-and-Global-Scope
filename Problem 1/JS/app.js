function calculateTotal() {
    let price = 100;
    console.log(price);
}
calculateTotal()

// Cannot console log variable inside function from the outside
// console.log(price);

// Declare variable outside of function for it to not give an error
let price = 100;
console.log(price);